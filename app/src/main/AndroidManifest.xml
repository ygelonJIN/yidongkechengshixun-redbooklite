<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RedBookApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_post"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_post"
        android:supportsRtl="true"
        android:theme="@style/Theme.RedBookLite"
        tools:targetApi="31">
        <activity
            android:name=".ui.detail.NoteDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.author.AuthorActivity"
            android:exported="false" />
        <activity
            android:name=".ui.publish.PublishActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RedBookLite">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
